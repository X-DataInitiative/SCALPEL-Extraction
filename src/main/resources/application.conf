environment = "test"

pioglitazone = {
  include "config/pioglitazone/pioglitazone.conf"
}

test = {
  include "test.conf"
}

polytechnique = {
  include "polytechnique.conf"
}

cnam = {
  include "cnam.conf"
}