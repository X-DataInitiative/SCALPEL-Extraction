package fr.polytechnique.cmap.cnam.utilities

import fr.polytechnique.cmap.cnam.SharedContext

/**
  * Created by burq on 11/07/16.
  */
class FlatteningConfigSuite extends SharedContext {
// todo : everything

  // "getTableConfig" should "return the right config file"
  // it should fail when the table name doesn't exists
  // it should fail when two items with the same name exists
}
