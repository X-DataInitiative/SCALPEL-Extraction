exposures = {
  min_purchases = 1
  purchases_window = 0
  start_delay = 0
  period_strategy = "unlimited"
  weight_strategy = "purchase-based"
  cumulative = {
    window = 1 #Number of months to quantile.
    start_threshold = 6 #Number of months within which more than one purchases have to made
    end_threshold = 4 #Number of months during which no purchases of the particular molecule have to be made
  }
}

cox_parameters = {
  exposures = {
    min_purchases = 1
    purchases_window = 0
    start_delay = 0
    exposure_type = "PurchaseBasedCumulative"
    cumulative = {
      window = 1 #Number of months to quantile.
      start_threshold = 6 #Number of months within which more than one purchases have to made
      end_threshold = 4 #Number of months during which no purchases of the particular molecule have to be made
    }
  }
}